# 최대 600,000글자까지 입력
# 커서
# 문장의 맨 앞 (첫번째 문자의 왼쪽)
# 문장의 맨 뒤 (마지막 문자의 오른쪽)
# 문장 중간 임의의 곳(모든 연속된 두 문자 사이)
import sys
input = sys.stdin.readline

# word = input()
# num = int(input())

# cur = -1
# while num >= 1:
#     a, b = input().split()
#     if a == 'P': # P $ : $라는 문자를 커서 왼쪽에 추가함

#     elif a == 'L': # L : 커서를 왼족으로 올림 (커서가 문장의 맨앞이면 무시)

#     elif a == 'D': # D : 커서 왼쪽에 있는 문자를 삭제 (가)
    
#     else: # B : 커서 왼쪽에 있는 문자 삭제 (커서가 문장 맨 앞이면 무시된)
